class Strings {
    constructor(data = {}) {
      // DONT DELETE THESE DATA OBJECTS 
      this.share = data.share || 'Share the App via..'; 
      this.message = data.message || 'Scan to share the QR code.'; 
      //ADD NEW ONES BELOW
    }
  }